server:
  port: 8080
  shutdown: graceful

spring:
  application:
    name: dify-to-agentscope
  lifecycle:
    timeout-per-shutdown-phase: 20s

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      probes:
        enabled: true

logging:
  pattern:
    level: "%5p [traceId:%X{traceId:-},workflowId:%X{workflowId:-}]"

dify:
  agentscope:
    default-workflow-id: default
    generated-output-dir: target/generated-workflows
    workflows:
      default:
        source-type: dify
        dsl-location: classpath:workflows/default.dsl
      manual-demo:
        source-type: agentscope
        location: classpath:workflows/manual-demo.workflow.yml
    build-agents: false
    fail-on-missing-integrations: false
    request-timeout: 10s
    deployment:
      stateless: false
      write-generated-artifacts: true
      node-executor-threads: 8
      node-executor-queue-capacity: 1000
      allow-in-memory-state: true
    persistence:
      type: memory
      key-prefix: dify-to-agentscope
      session-ttl: 7d
      a2a-task-ttl: 1d
      agent-state-ttl: 7d
    workflow-store:
      type: memory
      nacos-data-id-prefix: dify-agentscope-workflow-
      nacos-index-data-id: dify-agentscope-workflows.json
      nacos-group: DEFAULT_GROUP
    execution:
      max-steps: 100
      default-node-timeout: 30s
      node-timeouts: {}
      fallback-answer: "当前系统繁忙，无法联网工作，请稍后重试！"
      identifier-rules:
        demo-item: "ITEM-\\d{4}"
        demo-request: "REQ-\\d{4}"
        demo-task: "TASK-\\d{4}"
    memory:
      session:
        enabled: true
        max-turns: 20
      memos:
        enabled: false
        endpoint:
        access-token:
        visibility: PRIVATE
        default-limit: 5
    nacos:
      enabled: false
      server-addr:
      namespace: public
      username:
      password:
      access-key:
      secret-key:
      prompt:
        enabled: true
        keys: []
        version:
        label:
      skill:
        enabled: true
        names: []
        version:
        label:
      mcp:
        enabled: true
        servers: {}
    boundary:
      max-query-length: 4000
      max-output-length: 8000
      require-user-id: true
      allow-stub-integrations: true
